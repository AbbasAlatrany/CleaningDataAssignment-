the script first load the data set into variables.
Then we merge the files to make one data set, all the traning nd testing files.
We add variables names to our data to make it clear what each cloumn is

Read the feature names and assign them to corresponding cloumns
Make the feature names clearer by change each agg example, t for time

make a new data set by grouping by ativity and subject and caualte the mean of each feature.

finally save the tidy data set.